import * as C from '../../src/topics/concepts';
import { ToDo } from '../../src/utils/predef';

describe('Concepts', () => {
  describe('pure function', () => {
    it('Run test and check the output in console', () => {
      C.runTest();
    });
  });
});
